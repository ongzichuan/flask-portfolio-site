trigger:
  branches:
    include:
      - main

pool:
  name: Default

variables:
  dockerHubRepo: 'ongzichuan/flask-k8s-project'
  imageTag: '$(Build.BuildId)'

stages:
  - stage: Build
    displayName: Build and Push Docker Image
    jobs:
      - job: Build
        steps:
          - checkout: self

          - task: Docker@2
            displayName: Login to Docker Hub
            inputs:
              command: login
              containerRegistry: 'dockerhub-service-connection'

          - task: Docker@2
            displayName: Build and Push Image
            inputs:
              command: buildAndPush
              repository: '$(dockerHubRepo)'
              dockerfile: 'app/Dockerfile'
              buildContext: '.'
              tags: |
                $(imageTag)
             

  - stage: Deploy
    displayName: Deploy to Kubernetes
    dependsOn: Build
    jobs:
      - job: Deploy
        steps:
          - checkout: self

          - script: |
              sed -i "s|ongzichuan/flask-k8s-project:.*|ongzichuan/flask-k8s-project:$(imageTag)|g" k8s/deployment.yaml
              kubectl apply -f k8s/deployment.yaml
              kubectl apply -f k8s/service.yaml
              kubectl rollout restart deployment/flask-k8s-app
              kubectl rollout status deployment/flask-k8s-app
            displayName: Deploy to Kubernetes
